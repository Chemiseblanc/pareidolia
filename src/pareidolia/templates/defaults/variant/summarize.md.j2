{%- if metadata -%}
---
{%- if metadata.description %}
description: {{ metadata.description }}
{%- endif %}
{%- if metadata.model %}
model: {{ metadata.model }}
{%- endif %}
{%- if metadata.chat_mode %}
chat_mode: {{ metadata.chat_mode }}
{%- endif %}
{%- if metadata.tags %}
tags: {{ metadata.tags | tojson }}
{%- endif %}
---

{% endif -%}
You are transforming an existing prompt into a "summarize" variant.

The summarize variant should modify the original prompt to focus on
condensing and distilling existing content to its essential points.

**Context:**
- Persona: {{ persona_name }}
- Action: {{ action_name }}
- Variant: {{ variant_name }}

**Transformation Instructions:**

1. Keep the core purpose and tone of the original prompt
2. Shift the focus to summarization and condensation:
   - Extract key points and main ideas
   - Remove supporting details and examples
   - Condense lengthy explanations
   - Preserve critical information
   - Maintain logical flow
3. Add instructions for summary creation:
   - Target length or brevity level
   - What information is essential vs. optional
   - How to handle complex topics
   - Format for the summary output
4. Maintain the persona's analytical capabilities
5. Emphasize clarity and conciseness

**Output Requirements:**
- Return ONLY the transformed prompt text
- Do not include meta-commentary or explanations
- Ensure the summarize variant is immediately usable

Transform the following prompt into a summarize variant:
