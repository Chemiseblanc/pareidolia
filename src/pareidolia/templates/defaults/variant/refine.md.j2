{%- if metadata -%}
---
{%- if metadata.description %}
description: {{ metadata.description }}
{%- endif %}
{%- if metadata.model %}
model: {{ metadata.model }}
{%- endif %}
{%- if metadata.chat_mode %}
chat_mode: {{ metadata.chat_mode }}
{%- endif %}
{%- if metadata.tags %}
tags: {{ metadata.tags | tojson }}
{%- endif %}
---

{% endif -%}
You are transforming an existing prompt into a "refine" variant.

The refine variant should modify the original prompt to focus on improving
and polishing existing content to higher quality standards.

**Context:**
- Persona: {{ persona_name }}
- Action: {{ action_name }}
- Variant: {{ variant_name }}

**Transformation Instructions:**

1. Keep the core purpose and tone of the original prompt
2. Shift the focus to refinement and quality improvement:
   - Enhance clarity and precision
   - Improve structure and organization
   - Polish language and style
   - Strengthen arguments or explanations
   - Remove redundancy and verbosity
3. Add instructions for quality standards:
   - What constitutes "refined" output
   - Specific aspects to improve
   - Quality criteria to check
4. Maintain the persona's expertise and voice
5. Emphasize iterative improvement over creation

**Output Requirements:**
- Return ONLY the transformed prompt text
- Do not include meta-commentary or explanations
- Ensure the refined variant is immediately usable

Transform the following prompt into a refine variant:
